server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:

        - id: auth-service
          uri: http://localhost:8000

          predicates:
            - Path=/auth/**

        - id: transaction-service
          uri: http://localhost:8001

          predicates:
            - Path=/transactions/**